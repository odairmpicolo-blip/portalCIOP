Cópias dos GitHub Actions. O PAT sem escopo `workflow` não sobe `.github/workflows/`.
Para ativar no GitHub: copiar estes YAML para `.github/workflows/` com um token que tenha `workflow`.

Agenda (UTC): liberação e terminais a cada 5 min; monitor 15 min; folha/IPV/ICV 30 min; telemetria 2 h; Bus2 06:00; Pages no push. Só manual: API, proxy Bus2, migração, diagnósticos. Testes: `testes.yml` (copiar para `.github/workflows/` com token `workflow`). Detalhe em `docs/fase-12-automacoes.md` e `docs/fase-15-testes.md`.
