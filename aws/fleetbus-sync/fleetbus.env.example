# Copie este arquivo para $HOME/.config/ciop-portal/fleetbus.env
# e preencha com o refresh_token capturado no DevTools do navegador
# (aba Network, POST /oidc/oauth2/token, corpo da resposta -> "refresh_token").
#
# Depois rode: bash scripts/deploy-fleetbus-lambda.sh
FLEETBUS_REFRESH_TOKEN=
