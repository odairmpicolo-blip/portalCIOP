services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: portal_user
      POSTGRES_PASSWORD: portal_dev
      POSTGRES_DB: portalciop
    ports:
      - "5432:5432"
    volumes:
      - portalciop_pg:/var/lib/postgresql/data

volumes:
  portalciop_pg:
